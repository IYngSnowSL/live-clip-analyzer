# Changelog

## v0.5.8 — 修复批量转写命令重复传输入目录导致 unrecognized arguments

- v0.5.5 引入的 `_batch_transcribe` 在 make_cmd 已追加输入目录的情况下，
  又追加了一次 `--skip <目录>`——目录出现两次，第二次被 exe 判为
  unrecognized arguments（退出码 2），批量转写从未真正跑起来
- 修复：只追加布尔开关 `--skip`（目录由 make_cmd 提供）
- 实测验证（与生产完全一致的参数）：目录批量一次转完 2 块；
  --skip 重跑仅补缺失块（12.5s），既有输出跳过
- 版本 0.5.8

## v0.5.7 — 修复 --model_preload 参数错误导致的转写启动失败

- v0.5.6 引入的 `--model_preload` 实为**需带值的参数**（none_or_str2bool），
  裸传导致 argparse 吞掉输入目录、转写启动即失败（退出码 2）
- 移除该参数（批量模式本身只加载一次模型，预加载收益甚微）；
  错误输出机制（v0.5.3）正确显示了完整原因，快速定位

## v0.5.6 — 借鉴 MAW（moys-asr-workflow）的 ASR 经验：自动语言 + 质量参数 + 热词

- **语言改自动检测**：不再固定 `-l zh`，exe 自动识别（JSON 的 language 字段回读并打日志），
  中日混播内容自动适配（借鉴 MAW WhisperEngine 的做法）
- **关闭跨窗上下文**：`--condition_on_previous_text false`——MAW 经验：长音频中
  一句幻觉会被跨 30 秒窗口持续放大，关闭后更稳（质量提升、无速度代价）
- **VAD 静音切分对齐 500ms**：`--vad_min_silence_duration_ms 500`（MAW 经验值，
  静音切分更细、字幕密度更高）
- **热词功能**：新配置 `asr.local_hotwords`（空格分隔），专名/梗词识别增强，
  走 exe 的 `--hotwords`
- **音频块加大到 30 分钟**：`asr.chunk_seconds` 1200 → 1800（折中方案：
  保留 --skip 失败隔离，文件数减半、模型加载次数减半）
- README 致谢表补充 MAW（AGPL-3.0）——仅借鉴思路与参数值，未复制代码

## v0.5.5 — 本地 ASR 效率优化：批量模式 + 动态批解码

- **目录批量模式**：一次 exe 调用转完任务的全部音频块，模型只加载一次
  （此前每块单独启动 exe、每块重新加载 3GB 模型）
- **--batched 动态批解码**（默认开启）：whisper-standalone 的加速特性，
  解码阶段显著提速；新配置 `asr.local_batched`（默认 true）
- **beam_size 可配**：`asr.local_beam_size`（默认 5；1=最快、质量略降）
- **--skip 增量重试**：批量失败重试时只补跑缺失输出的块（容错粒度不变），
  最后一次重试自动降级为非 batched（显存更省更稳）
- **--model_preload**：批量模式下模型预加载更顺滑
- 保留：显存预检等待、强制 CUDA（默认不回退 CPU）、串行锁、每块偏移累计、
  卡卡式精细化

## v0.5.4 — 本地 ASR 强制 CUDA（同块重试 + 显存预检，不再默认回退 CPU）

- **策略调整**：默认**强制 CUDA**——CUDA 失败不再回退 CPU，改为同一块
  **最多重试 3 次**（间隔 20s/40s），仍失败才跳过该块（长视频任务时长可控）
- **显存预检**：每次运行前查询空闲显存（nvidia-smi），不足保守阈值
  （float16 约 4.4GB / int8 约 2.6GB）时自动等待释放（最长 10 分钟），
  避免与桌面应用争抢显存导致的瞬时 OOM
- **CPU 回退降级为可选**：新配置 `asr.local_fallback_cpu: false`（默认）；
  置 true 才在 CUDA 多次失败后回退 CPU 兜底

## v0.5.3 — 本地 ASR 容错加固（CUDA 失败自动回退 + 完整错误输出）

- **修复错误详情丢失**：独立程序很多错误打在 stdout 而非 stderr，此前只保留
  stderr 尾部导致「转写失败」查无原因；现在 stdout/stderr 尾部都保留并附退出码
- **CUDA 回退条件放宽**：不再依赖错误关键词匹配，CUDA 上任何失败都自动回退
  CPU 重跑该块（实测：同一音频块 CPU 转写正常，证明失败属 CUDA 瞬时故障，
  如显存竞争，而非文件损坏）

## v0.5.2 — 本地 ASR 改为独立程序子进程方式（复刻卡卡字幕助手）

- **根治 CUDA 转写死锁**：v0.5.1 之后实测发现，卡死发生在进程内 Python 库
  ctranslate2 的 CUDA 推理首次调用（非并发问题，CPU 路径完全正常）。
  本版把本地 ASR 引擎改为**子进程调用独立程序 `faster-whisper-xxl.exe`**
  （whisper-standalone-win，卡卡字幕助手 VideoCaptioner 同款做法）：
  转写崩溃/卡死完全隔离在子进程，主服务不再被冻结
- **参数复刻 VideoCaptioner**：`-l zh` 固定中文、VAD 阈值 0.4、
  词级时间戳 + 本项目卡卡式断句精细化（JSON 输出解析）
- **容错**：CUDA 块失败自动回退 CPU 重试该块；单块失败跳过；串行锁保留
- **新配置**：`asr.local_whisper_bin`（exe 路径，留空自动探测 VideoCaptioner 目录/PATH）、
  `asr.local_vad_threshold`（默认 0.4）；`local_compute_type` 默认改为 `default`
  （交给 exe 自动选择）
- **文档**：README 新增「致谢与开源协议」——VideoCaptioner（GPL-3.0）与
  whisper-standalone-win（MIT）的借鉴说明；requirements.txt 移除 Python 版
  faster-whisper 安装指引（不再需要）

## v0.5.1 — 修复本地 ASR 多任务并发转写死锁

- **修复严重 bug**：批量任务并发转写时，多个任务在同一共享 WhisperModel 实例上
  同时调用 `transcribe()`（faster-whisper 默认 `num_workers=1` 不支持真并行），
  实测出现进程级死锁——3 个任务全部线程 Wait、零 CPU/GPU 活动、40 分钟无产物
- **修复方式**：`local_asr.py` 增加进程内串行锁（同一时刻只允许一个转写调用；
  单 GPU 上 beam-5 转写本就无法真并行，串行化几乎不损失吞吐）+ 模型加载双重检查锁
  （防并发重复加载 3GB 模型导致显存翻倍）

## v0.5.0 — UI 双主题与操作效率优化

- **深色/浅色双主题**：右上角 🌙/☀️ 一键切换，localStorage 持久化，首帧前应用无闪烁；
  浅色主题全套令牌重调（背景/表面/边框/文字/阴影/光晕），`color-scheme` 跟随系统控件
- **报告页粘性工具栏**：标题 / 状态 / 导出按钮 / 标签页吸顶（毛玻璃背景），
  长轴列表滚动时操作始终可达
- **评分分级着色**：轴卡片左侧色条与评分徽章按 9~10 金 / 7~8 绿 / 5~6 灰着色，
  高光片段一眼扫出
- **任务列表**：空状态提示、标题计数（任务列表 · N）、运行中进度条流光动画 + 百分比
- **轴列表**：最低评分筛选实时计数（显示 X / Y 个轴）；区分「暂无切片轴」与「筛选无结果」空状态
- **字幕页**：复制字幕文件路径按钮 + 句数统计
- **小功能**：首页视频路径多行粘贴自动解析为多选 chips；报告窗口滚动后显示「回到顶部」按钮
- **视觉打磨**：首页背景主题化柔和光晕；聚焦光环 / 主按钮光晕 / 遮罩层全部令牌化；滚动条悬停反馈

## v0.4.9 — 审查修复（P2/P3）+ 安全加固与打磨

### 安全加固（P2）
- **回环地址保护**：绑定非回环地址（如 0.0.0.0）必须显式设置
  `server.allow_non_localhost: true`，否则拒绝启动（无鉴权文件浏览/连通测试不再可能意外暴露）
- **连通测试禁重定向**：`test-connection` 不再跟随重定向（防 SSRF 借重定向探测内网）
- **CSV 公式注入防护**：以 `= + - @` 开头的标题/理由单元格前置单引号

### 打轴逻辑（P3）
- 最短轴时长可配：`axle.min_axle_seconds`（默认 20 秒，替代硬编码阈值）
- score < 5 的内容点强制过滤（不再只靠提示词自觉）
- 时间戳越界防护：LLM 幻觉出的 end 截断到视频总时长
- 静音精修防护：不把边界推出原始范围，调整后不足 5 秒放弃该侧调整（不再压成 1 秒碎片）
- 尾部未闭合静音以视频总时长补全（不再丢失最后一个 silence_start）

### 健壮性（P3）
- `extract_json` 改用增量解析器：支持数组根与多 JSON 场景，不再贪婪误匹配
- AI 客户端 timeout / concurrency 参数钳制（10s~1h / 1~32）
- `update_task` 列名白名单，杜绝任意列注入
- `offset_seconds` 校验（非负有限数值）
- 任务全局并发上限 4（批量 20 个不再同时打满 CPU/网络）
- asr.json 读写与 SRT 生成移入线程池（不阻塞事件循环）
- `init_db` / 中断任务标记移入 FastAPI lifespan（不再有导入期副作用）
- `fine_mode` 不再泄漏进打轴参数
- media-info 返回失败原因（不再静默吞掉 ffprobe 异常）

### 前端（P3）
- `api()` 支持按调用指定超时；切片导出放宽到 10 分钟（不再 45s 假失败）
- 创建任务：手动输入与已选文件合并；单选走单任务接口（自动弹幕提示生效）；批量统一弹幕时明确提示
- 复核保存：前后端双重校验起止时间 + 防重复提交
- 评分筛选、字幕状态随任务切换重置；模型下拉点击不再被误关；页面切后台暂停任务轮询
- 复制成功提示改为独立 toast（不再被进度轮询覆盖）

### 清理（P3）
- 删除死代码：`extract_frames` / `describe_images` / `image_to_data_url` /
  `export_clip_sync` / vision 客户端无条件创建 / 冗余 import / 死 CSS
- 删除已删模块的陈旧 `.pyc` 与 data 内测试残留 SRT
- requirements.txt 显式声明 pydantic，移除过时的 scenedetect 注释
- 环境变量覆盖补齐：`ai.endpoints.*`、`asr.engine / local_model_path / local_device / local_compute_type`
- ADR-0003 标 superseded、0005 标 partially superseded、0004/0006 补修订说明
- SRT 序号连续化；数字小数点不再被误替换成逗号

## v0.4.8 — 本地 ASR 切换 CUDA + 全项目审查修复（P0/P1）

- **本地 ASR 切换 CUDA**：默认 `asr.local_device: cuda`、`local_compute_type: float16`；
  CUDA 不可用自动回退 CPU int8（`ctranslate2.get_cuda_device_count` 检测），加载时打印实际设备
- **删除任务取消在飞任务**：强制删除运行中任务时先 cancel 对应后台协程，
  不再出现删除后 ffmpeg 继续跑、LLM/ASR 继续计费、目录被重建的孤儿执行
- **AI 客户端传输层重试**：httpx 连接/超时异常包装为 `AIError` 并按指数退避重试，
  打轴不再因单次网络抖动整体失败
- **本地 ASR 单块兜底**：单块转写失败跳过继续（与云端引擎行为一致），不中断整个任务
- **重新打轴补弹幕峰值**：reaxle 后弹幕高峰标注不再丢失（抽公共函数复用）
- **长度口径统一**：LLM 找点提示词按当前配置动态生成，不再硬编码「最多 10 分钟」；
  CONTEXT / ADR-0006 补修订说明
- **ffmpeg 取消清理**：协程被取消时 kill 子进程并回收，避免孤儿 ffmpeg 进程
- **前端竞态守卫**：报告切换/轮询加 token 校验（快速切换任务不再渲染错配数据），
  字幕标签页状态随任务切换重置
- 数据清理：删除 axles 表孤儿行（无主 task 的 23 行）与遗留任务目录

## v0.4.7 — 配置页修复与体验打磨

- 修复 API key 输入框不可清空的问题（可显式清空回退到上级配置）
- 视觉模型 ping 失败时回退全局接口测试
- 设置弹窗 sticky footer 与 details 面板布局压缩修复
- 字幕每行最大字符数提示

## v0.4.6 — 真实模型下拉与文件浏览器增强

- 模型下拉真正可用：测试连接后按 文本/视觉/ASR 分类筛选模型并计数
- 文件浏览器显示视频时长、收藏常用路径

## v0.4.5 — 本地 Faster-Whisper 转录（继承卡卡字幕助手）+ 重新打轴读新配置

- **本地 ASR 引擎**（`app/services/local_asr.py`）：faster-whisper + 内置 Silero VAD
  （`vad_filter`），复用卡卡字幕助手已下载的 large-v2 模型（默认路径
  `D:\AdobE\VideoCaptioner\AppData\models\faster-whisper-large-v2`），零 API 费用
- **语言处理**：逐音频块自动检测，仅在中文/日文间二选一（Vtuber 中日混播友好）
- **卡卡式字幕精细化**：词级时间戳 + 中日标点断句 + 每行 ≤30 字符（可配
  `asr.subtitle_max_chars`），提前断句保证不超宽；无词级时间戳时按字符比例插值兜底
- **引擎可切换**：设置页选择「本地 faster-whisper（默认）」/「云端 API」，
  云端路径保留（硅基流动 SenseVoiceSmall）
- **重新打轴自动使用新配置**：面板打开时读当前生效的打轴参数（设置页保存即生效），
  不再硬编码 30/3600
- 修复 Anaconda OpenMP 冲突（`KMP_DUPLICATE_LIB_OK`，ctranslate2 与 numpy 重复加载
  libiomp5md.dll 导致进程崩溃）
- 实测（用户真实直播 60 秒音频）：CPU int8 转写 34 秒（约 1.8 倍实时），
  12 句精细化字幕全部 ≤30 字符，时间戳 1~6 秒级

## v0.4.4 — UI 优化（条目归属修正见下）

- **A. 一键复制剪辑标记**：单轴「复制」按钮（`HH:MM:SS - HH:MM:SS 标题`）；
  顶部「📋 复制全部」一键复制全部轴清单（**制表符分隔 TSV**，可直接粘贴进 Excel / 共享表格）
- **B. 弹幕高峰标注**：重新实现流式弹幕解析（大文件低内存，`app/services/danmaku.py`）；
  任务关联弹幕时自动计算每分钟密度，每个轴标注弹幕高峰时刻（`axles.danmaku_peaks`，
  可点击跳转视频预览）
- **D. 字幕搜索**：字幕页搜索框——匹配条目列表（带时间戳可点击跳视频）+ 全文高亮
- 修复：显式传入的弹幕路径被自动检测逻辑丢弃的 bug（v0.4.1 引入）
- 注：复制标记 / 弹幕高峰 / 字幕搜索三项实为 v0.4.2 引入（见下），本版其余 UI 优化未单列

## v0.4.3 — 重新打轴入口调整 + 理由增强

- 「重新打轴」按钮移到任务列表条目（查看/删除旁），操作更顺手
- 打轴理由引用更多原文素材：带时间戳的引用行 + 用途标签（开场/标题/结尾/笑点）

## v0.4.2 — 复制标记、弹幕高峰与字幕搜索

- 一键复制剪辑标记：单轴「复制」按钮（`HH:MM:SS - HH:MM:SS 标题`）；
  顶部「📋 复制全部」一键复制全部轴清单（**TSV 制表符分隔**，粘贴进 Excel / 共享表格直接成表）
- 弹幕高峰标注：重新实现流式弹幕解析（大文件低内存），关联弹幕后每个轴标注
  弹幕密度峰值时刻（🔥 可点击跳转视频预览）
- 字幕搜索：匹配条目列表（带时间戳可点击跳视频）+ 全文高亮

## v0.4.1 — 字幕优化与体验打磨

- **SRT 字幕格式优化**：无表情、句号转逗号做句读、句尾不带句号、保留情感标点（？！），
  可直接插入剪辑软件；存放于视频同目录 `[创建日期]_视频名` 文件夹
- **WebUI 字幕文件页**：报告窗口新增「字幕文件」标签页，在线预览完整 SRT（新增 `GET /api/tasks/{id}/subtitle`）
- **自动弹幕关联**：创建任务（单/批量）时自动查找视频同目录同名 `.xml` 并关联，前端弹窗提示
- **理由更详细**：`FIND_POINTS_PROMPT` 要求理由引用片段内 1~2 个具体时间戳与原话举例

## v0.4.0 — 六项功能更新

1. 文件浏览器固定窗口大小（进出目录不变形）+ 勾选多选视频
2. **重新打轴**：复用 ASR 结果不重复计费，可改目标时长，可选精细分窗模式（`POST /api/tasks/{id}/reaxle`）
3. **批量任务**：`POST /api/tasks/batch` 一次创建多任务并行分析（最多 20 个），首页 chips 标签
4. **评分说明**：0~10 分含义图例（ℹ️ 按钮展开）
5. SRT 字幕附属文件：ASR 完成后自动生成（时间戳+文本），存视频同目录
6. `tasks.srt_path` 字段 + 报告页字幕位置提示

## v0.3.2 — API 配置增强

- 设置页 **API 推荐卡片**（硅基流动一家通吃 + DeepSeek 备选，标注推荐日期 2026-09-02）
- **高级设置**：文本/视觉/ASR 三模型各自独立 base_url/api_key（`ai.endpoints`，空值继承全局）
- **连通测试**：`POST /api/config/test-connection` ping `/models`（兼容 `/v1` 前缀），
  返回模型列表供前端下拉选择；全局接口与三个模型组各有测试按钮
- AI 门面：三种能力各自独立 httpx client
- 打轴默认值放宽：30 秒 ~ 1 小时
- 修复精切勾选框偏移；修复占位符 key 伪装成掩码导致用户误以为已配置的问题

## v0.3.1 — 深色 DeepSeek 风格 UI 重设计

- 首页：居中标题+两个输入栏+开始按钮，右上角设置，左下正方格任务列表
- 报告页：嵌套窗口 overlay 覆盖，首页内容收进窗口左侧栏，切换带过渡动画

## v0.3.0 — 推倒重建：自动打轴为唯一核心

按用户重新定义的产品定位（切片生产者，产物用于二创发布）推倒重建（ADR-0006）。

### 核心：自动打轴
- 新流水线：ffprobe → 音频提取 + 转封装 → ASR 转写（缓存）→ LLM 分窗找内容点并初步打轴
  → 内容点合并 + 长度约束（目标 1~3 分钟，硬上限 10 分钟）→ **静音点精修**（ffmpeg silencedetect）
  → axles 表保存
- 每个轴输出：精确起止时间 + 标题 + 推荐理由 + 评分
- 大量候选（默认最多 100 个），按评分排序，人工挑选
- 新增 `app/services/axle.py`（核心）、`app/api/axles.py`（列表 / 复核 / CSV）
- **CSV 导出**（BOM，直接对照剪辑软件打轴）
- 人工复核（起止时间 / 标题），导出以复核值为准

### 删除（判为臃肿）
- 视觉场景检测与 8~30 秒时间轴、画面理解（视觉模型调用）
- 弹幕统计、话题段（粗切）、综合加权评分
- 中英双语报告（改为中文轴清单 + CSV）
- 移除模块：scene_detect / vision / danmaku / timeline / scorer / candidates / segmentation / subtitle / report

### 保留
- 任务管理、ASR、AI 门面（LLM / 视觉 / ASR 能力接口）、切片导出（快切 / 精切）、
  视频预览跳转、文件浏览、WebUI 配置页（密钥脱敏）

## v0.2.1 — 配置安全与健壮性

（已废弃，见 v0.3.0）

## v0.2.0 — DeepSeek UI Release

（已废弃，见 v0.3.0）
