fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27
pydantic>=2.0
pyyaml>=6.0

# 本地 ASR 引擎（可选，按需安装，见 README「安装」一节）：
#   CPU 版：pip install faster-whisper
#   CUDA 版：pip install "ctranslate2[cuda12]" faster-whisper
