fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27
pydantic>=2.0
pyyaml>=6.0

# 繁→简转换（可选，强烈推荐）：Whisper 系模型识别中文常输出繁体，
# 装上后字幕/打轴文本按词组级准确转简体；未安装时用内置常用字表兜底。
zhconv>=1.4

# 本地 ASR 引擎：外部独立程序 faster-whisper-xxl.exe（whisper-standalone-win），
# 以子进程方式调用，无需安装 Python 版 faster-whisper。
# 可复用卡卡字幕助手 VideoCaptioner 已安装的程序与模型（见 README「本地 ASR 引擎」）。
