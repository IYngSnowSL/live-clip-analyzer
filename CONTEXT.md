# CONTEXT.md — 领域术语表

本文档是 live-clip-analyzer 的领域词汇表，只记录术语的准确定义，不包含实现细节。
当你在代码、文档或讨论中使用这些词时，请与本文保持一致。

## 核心对象

- **任务（Task）**：一次完整的分析作业。输入一个本地视频文件（可附带 B 站弹幕 XML），
  产出时间轴、切片候选与报告。任务有生命周期状态：`pending → running → done / failed`。
- **场景 / 镜头段（Scene）**：时间轴的基本单元，存放于 `scenes` 表。视频被切分成若干
  连续时间段（默认 8~30 秒），每个场景拥有独立的中英双语标题、概述、画面描述、
  语音转写、弹幕统计与评分。**注意**：本项目中"场景"专指镜头段，
  与规划中的"话题段"是两个不同层级的概念，不要混用。
- **话题段（Topic Segment）**：字幕驱动切分的主功能产物，存放于 `topic_segments` 表。
  由字幕（ASR 全文）语义驱动切分，颗粒度为几分钟~几十分钟，回答"这一段在聊什么事"，
  是内容导航层（类比书的目录），不作为切片直接导出。已实现粗切（纯 Python 预筛 + LLM 命名）。
- **切片友好片段（Clip Segment）**：规划中的细切层产物，30 秒~3 分钟，
  有起承转合、切出来即可发布；落地为 `candidates` 表的候选类型，走"复核→导出"链路。
- **时间轴（Timeline）**：全部场景（镜头段）按时间顺序组成的完整列表，是分析报告的主体。
- **候选（Candidate）**：值得导出为视频切片的建议。分两种类型，见下。
- **长切片候选（Long Candidate）**：时长 1~5 分钟的完整切片建议，
  由相邻高分场景按允许间隔合并而成，用于直接发布。
- **单句素材候选（Sentence Candidate）**：来自语音转写的一句原话（5~50 字），
  自带起止时间戳，适合做二创梗、表情包或短视频素材，不一定剪整段视频。
- **复核（Review）**：用户在 Web 界面对候选的人工微调（起止时间、标题、评分、推荐等级）。
  复核值一旦设置即覆盖 AI 生成值，后续报告与导出均以复核值为准。
- **导出（Export）**：把候选按起止时间裁剪成独立视频文件（MP4）的动作。

## 评分体系

- **综合评分（final_score）**：0~10 分。由四个子分数加权求和：
  弹幕热度、弹幕情绪、内容有趣度、高光潜力。权重可在配置中调整。
- **推荐等级（Rank）**：`high / medium / low` 三档，由综合评分阈值决定
  （默认 ≥7.5 为 high，≥5 为 medium，其余 low）。等级由评分唯一决定，不由模型直接输出。
- **弹幕热度（Danmaku Heat）**：0~1。场景内弹幕密度相对全片最大密度的归一化值。
- **弹幕情绪（Danmaku Emotion）**：0~1。场景内情绪关键词命中率的估算值（3 倍封顶）。
- **有趣度（fun_score）**：0~10。由 LLM 依据画面、语音与弹幕综合判断。
- **高光潜力（highlight_score）**：0~10。由 LLM 判断名场面 / 二创潜力。

## 输入与中间产物

- **弹幕偏移（offset_seconds）**：弹幕时间轴与视频时间轴的偏差秒数，
  用于对齐录播与弹幕（例如直播延迟导致的错位）。
- **ASR 转写（ASR）**：语音识别。长音频先切成固定时长块分别转写，再按时间戳合并成
  `[{start, end, text}]` 片段列表。单块失败不中断整场转写。
- **画面描述（visual_summary）**：视觉模型对场景内代表帧的简短文字描述。
- **单句引用（Quote）**：LLM 从场景语音转写中挑出的、最适合二创的一句原话，
  必须原样引用，附定位到的时间戳（quote_start / quote_end）。

## 视频处理

- **转封装（Convert）**：把源视频（如 FLV）以流复制方式无损改写为浏览器可播放的 MP4；
  若源编码不支持复制则退回重编码。源文件本身已是可播放 MP4 时跳过此步骤。
- **快速剪切（快切）**：导出时使用 `-c copy` 无损流复制，速度接近文件复制，
  但起点会对齐到最近关键帧，可能比指定时间略早。
- **精切（重编码）**：导出时使用 H.264 + AAC 重编码，起点精确到帧，但速度慢、体积可能变化。
- **场景边界（Boundary）**：场景检测输出的时间点列表，经最短/最长时长约束精炼后
  成为最终时间段。检测由视觉画面变化驱动（PySceneDetect 优先，FFmpeg 场景滤镜兜底，
  均不可用时退化为固定时长切分）。

## 相关文档

- 架构决策记录：`docs/adr/`
- 新功能设计讨论：`docs/features/`（含字幕驱动切分的完整决策记录）
