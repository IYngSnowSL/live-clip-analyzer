# CONTEXT.md — 领域术语表

本文档是 live-clip-analyzer 的领域词汇表，只记录术语的准确定义，不包含实现细节。
当你在代码、文档或讨论中使用这些词时，请与本文保持一致。

> 版本说明：v0.3.0 推倒重建后（ADR-0006），软件以「自动打轴」为唯一核心。
> 旧术语（场景 / 话题段 / 候选）已废弃，仅保留"轴"体系。

## 核心对象

- **任务（Task）**：一次完整的分析作业。输入一个本地视频文件，自动打轴后产出轴清单。
  生命周期：`pending → running → done / failed`。
- **轴（Axle）**：自动打轴的核心产物，存放于 `axles` 表。一个"轴"表示一个
  可切片的完整内容单元，包含：精确起止时间（start / end）、切片标题（title）、
  推荐理由（reason）、评分（score）。用户可对轴进行复核微调。
- **内容点（Content Point）**：LLM 从字幕中找出的"值得切"的时刻（名场面 / 趣点 /
  高光 / 爆点 / 话题），是轴的原料。相邻内容点按间隔合并成一个轴。
- **打轴（Axling）**：从录播中自动确定"从哪个时间到哪个时间可以切"的动作，
  包括起止时间判定、静音点精修与内容说明生成。本软件唯一核心功能。
- **复核（Review）**：用户对轴的微调（起止时间、标题）。复核值覆盖 AI 生成值，
  后续 CSV 与切片导出均以复核值为准。

## 打轴流程术语

- **ASR 转写（ASR）**：把整段音频转写为带时间戳的句子列表 `[{start, end, text}]`。
  是打轴的地基；结果缓存于任务目录的 `asr.json`，重跑不重复计费。
- **分窗（Window）**：LLM 找内容点时把长字幕按时间窗切块（默认 10 分钟一窗，
  1 分钟重叠），避免超出上下文限制、并支持并发。
- **长度约束**：对轴时长的规则——目标 30 秒 ~ 1 小时（`target_min/max_seconds`，可配），
  硬上限 1 小时（`hard_max_seconds`），超过硬上限自动拆分，过短碎片丢弃。
- **静音点精修（Silence Refinement）**：用 ffmpeg `silencedetect` 找出静音区间，
  把轴的起点/终点微调到最近的静音点，使切出来的片段头尾干净、不留杂音。

## 导出术语

- **CSV 导出**：轴清单导出为 CSV（序号 / 起止秒数 / 时长 / 标题 / 理由 / 评分），
  带 UTF-8 BOM 供 Excel 正确显示，可直接对照剪辑软件打轴。
- **快速剪切（快切）**：`-c copy` 无损流复制导出切片，速度接近文件复制，
  起点对齐关键帧可能略早。
- **精切（重编码）**：H.264 + AAC 重编码导出，起点精确到帧，速度慢。

## 相关文档

- 架构决策记录：`docs/adr/`（0006 推倒重建为当前架构基准）
- 配置说明：`config.yaml` 内注释、README「配置」章节
